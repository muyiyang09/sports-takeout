# AI 微服务开发文档 · #03 Loop 工程落地

> 版本：v1.0 · 2026-08-26
> **文档分类**：落地指南（Guideline） · **强制性**：建议遵循 · **用途**：指导把固定 DAG 升级为带分支/循环/HITL 的状态机
> 前置阅读：[#02 §5 控制流](./02-Agent工程能力地图.md#5-控制流control-flow) · [#02 §6 状态管理](./02-Agent工程能力地图.md#6-状态管理state-management)
> 涉及文件：`app/graphs/base.py` / `app/graphs/recommend_coach.py` / `app/schemas/coach_recommend.py` / `app/config.py` / `app/main.py` / `tests/`

---

## 0. 文档说明

### 0.1 目标

把当前 `recommend_coach` 从「固定 3 节点串行 DAG」升级为「带 4 类循环控制的状态机」：

1. **条件分支**：Node2 候选为空 → 回 Node1 放宽条件重抽
2. **重试循环**：Node1 LLM 抽 JSON 失败 → 累加修正提示重试 N 次
3. **Refine 循环**：Node3 推荐理由质量门控不合格 → 回 Node3 重写
4. **HITL 人工介入**：预留一个 `interrupt` 节点，为后续证书审核 Agent 铺路

### 0.2 范围与边界

| 项 | 是否改动 | 说明 |
|---|---|---|
| 对外 HTTP 契约（输入/输出 JSON） | ❌ 不动 | 向后兼容，旧前端/Java 后端无感 |
| Pydantic Schema 输出 | ❌ 不动 | `RecommendResult` 字段不变（新增字段为可选） |
| Graph 内部 State 字段 | ✅ 新增 | retry_count / relaxed_fields / quality_check 等 |
| 节点函数实现 | ✅ 重构 | 抽出"宽松度参数" + "质量门控函数" |
| 编译方式 | ✅ 改 | 挂 `RedisCheckpointer` + thread_id |
| 部署配置 | ✅ 改 | `.env` 加 `CHECKPOINT_TTL` / `MAX_RETRIES` / `QUALITY_MIN_SCORE` |

---

## 1. 现状回顾

### 1.1 当前 Graph 拓扑

```
START → extract_intent → retrieve_and_rank → generate_reason → END
```

特点：
- 纯串行，**无任何分支/循环**
- [base.py](../app/graphs/base.py) 写了 `ConditionalRouter` 但 `recommend_coach.py` **未调用 `add_conditional_edges`**
- 节点失败立即 fallback mock，**无重试**
- Node2 候选为空时硬塞 mock 教练，**无回退抽取**
- Node3 推荐理由无论质量都直接返回，**无门控**
- 无 Checkpointer，**进程崩了无法 resume**

### 1.2 四大缺口清单

| 缺口 | 触发条件 | 当前行为 | 期望行为 |
|---|---|---|---|
| G1 条件分支 | Node2 `filtered` 为空 | 硬塞 `_MOCK_COACHES` | 回 Node1 放宽条件（去掉 city / 降低 level）重抽，最多重试 2 次 |
| G2 重试循环 | Node1 LLM 返回非 JSON | 立即抛 RuntimeError | 累加修正提示重试 N 次（默认 2），仍失败才 fallback mock |
| G3 Refine 循环 | Node3 推荐理由不合格 | 直接返回 | 回 Node3 重写（带"上次输出 X，问题 Y"提示），最多重试 2 次 |
| G4 HITL | 预留 | 无 | 通过 `interrupt()` 暴露人工介入点（教练推荐本身不需要，但为后续证书审核 Agent 跑通基础设施） |

---

## 2. 设计思路：为什么这样设计而不是那样

### 2.1 为什么用 ConditionalRouter 而不是 if-else

LangGraph 中实现分支有三种方式，对比：

| 方式 | 代码 | 可读性 | 可观测性 | 推荐 |
|---|---|---|---|---|
| 节点内 if-else + 多入口 END | 节点函数里写分支逻辑 | 差 | 差（看不到分支） | ❌ |
| 节点返回下一节点名 | `return {"next": "node_a"}` | 中 | 中 | ⚠️ |
| **ConditionalRouter + add_conditional_edges** | 路由表声明式 | 好 | 好（拓扑图清晰） | ✅ |

**关键认知**：Graph 的拓扑结构应当**显式声明**而非藏在节点函数里。这样：
- Trace 工具能画出真实走向
- 改路由不需要改节点函数
- 新人看拓扑就能理解流程

### 2.2 为什么"先重试再 fallback mock"

当前实现是「LLM 失败立即 mock」，问题：
- 偶发性失败（网络抖动 / 模型抽风）也走 mock，质量降级
- 无日志区分"模型真挂了" vs "模型偶发抖动"

正确顺序：
```
LLM 调用失败
   ↓
重试 N 次（带退避 + 修正提示）  ←—— 区分偶发 vs 必败
   ↓ 仍失败
fallback mock + 日志告警       ←—— 必败才降级
```

### 2.3 为什么 Refine 用"质量门控函数"而不是"LLM 自评"

Refine 循环触发有两种方式：

| 方式 | 实现 | 成本 | 准确度 |
|---|---|---|---|
| LLM 自评（"你觉得这个理由好吗"） | 多一次 LLM 调用 | 高 | 中（LLM 评自己有偏好） |
| **规则门控函数**（长度/含数据/无空话） | 纯 Python 函数 | 0 | 高（规则明确） |

**原则**：能用规则判定的，绝不浪费 LLM。规则门控通过后才考虑 LLM 自评（更高阶场景）。

### 2.4 为什么教练推荐不需要 HITL 但仍要预留

HITL 不是为了教练推荐本身，而是为了**让 LangGraph 的 interrupt + Checkpointer 基础设施在本项目跑通**。后续证书审核 Agent 必须用 HITL（管理员最终确认）。

如果 Phase 1 不把这套基础设施跑通，Phase 5 写证书审核 Agent 时要同时解决「业务逻辑 + HITL 工程」两个问题，风险叠加。**先用工具有效的低风险场景跑通基础设施，再上高价值场景**——这是工程化的标准做法。

---

## 3. 落地方案：四个缺口逐一实现

### 3.1 缺口 G1：条件分支（Node2 空结果回 Node1 放宽）

#### 3.1.1 状态字段设计

在 `RecommendState` 新增字段：

```python
class RecommendState(TypedDict, total=False):
    # ... 原有字段 ...
    # 新增：循环控制
    retry_count: int                    # 当前已重试次数（初始 0）
    relaxed_fields: list[str]           # 已放宽的字段名（避免重复放宽同一字段）
    # 新增：路由控制
    branch: str                         # ConditionalRouter 路由 key
```

#### 3.1.2 放宽策略函数

按"代价从低到高"顺序放宽（先放宽便宜的字段，后放宽贵的）：

```python
# app/graphs/recommend_coach.py 新增
_RELAX_ORDER: list[tuple[str, str]] = [
    # (字段名, 放宽动作描述)
    ("min_rating", "去掉最低评分限制"),
    ("level", "去掉教练等级限制"),
    ("male_only", "去掉性别限制"),
    ("district", "去掉区域限制，仅保留城市"),
    ("city_name", "去掉城市限制，全国兜底"),
]

def _relax_intent(intent: dict, relaxed_fields: list[str]) -> tuple[dict, list[str], str | None]:
    """按顺序放宽一个未放宽过的字段。返回 (新intent, 新relaxed_fields, 放宽动作名/None)"""
    for field, action in _RELAX_ORDER:
        if field in relaxed_fields:
            continue
        if intent.get(field) is None:
            continue  # 本来就没限制，不用放宽
        new_intent = {**intent, field: None}
        new_relaxed = relaxed_fields + [field]
        return new_intent, new_relaxed, action
    return intent, relaxed_fields, None  # 全放宽完了
```

#### 3.1.3 ConditionalRouter 接入

```python
# app/graphs/recommend_coach.py 末尾的 Graph 编译段，原代码：
# _builder.add_edge(START, "extract_intent")
# _builder.add_edge("extract_intent", "retrieve_and_rank")
# _builder.add_edge("retrieve_and_rank", "generate_reason")
# _builder.add_edge("generate_reason", END)

# 改为：
from app.graphs.base import ConditionalRouter

MAX_RETRY = settings.max_retries  # 默认 2

_builder.add_edge(START, "extract_intent")
_builder.add_edge("extract_intent", "retrieve_and_rank")

# 关键改动：Node2 之后不再直连 Node3，而是走路由
router = ConditionalRouter(
    state_field="branch",
    mapping={
        "to_reason": "generate_reason",   # 候选非空 → 生成理由
        "relax": "extract_intent",         # 候选为空且 retry < MAX → 回 Node1 放宽
        "force_mock": "generate_reason",  # 候选为空且 retry == MAX → 强制用 mock 教练
    },
    default="generate_reason",
)
_builder.add_conditional_edges("retrieve_and_rank", router.route, router.edges())

_builder.add_edge("generate_reason", END)
```

#### 3.1.4 retrieve_and_rank 节点改造

在节点末尾根据 `filtered` 是否为空 + `retry_count` 决定写 `branch`：

```python
def retrieve_and_rank(state: RecommendState) -> dict[str, Any]:
    # ... 原打分逻辑 ...

    # 候选为空的兜底改造：不再硬塞 mock，而是走条件分支
    if not candidates:
        retry = int(state.get("retry_count", 0))
        if retry < MAX_RETRY:
            # 回 Node1 放宽条件
            new_intent, new_relaxed, action = _relax_intent(
                intent, state.get("relaxed_fields", [])
            )
            if action:
                logger.info("[Node2] 空候选，回 Node1 %s（retry=%d）", action, retry)
                return {
                    "intent": new_intent,
                    "retry_count": retry + 1,
                    "relaxed_fields": new_relaxed,
                    "branch": "relax",   # 路由到 extract_intent
                    "candidates": [],    # 显式清空
                }
        # 放宽完了仍空 → 强制塞 mock
        candidates = [_build_mock_candidate()]
        logger.warning("[Node2] 放宽 %d 次仍空，强制用 mock 教练", retry)

    # 候选非空 → 走 Node3
    return {
        "candidates": [c.model_dump() for c in candidates[:top_n]],
        "matched_course": matched_course,
        "over_budget": over_budget,
        "branch": "to_reason",   # 路由到 generate_reason
    }
```

注意：`extract_intent` 节点要识别 `state.get("intent")` 已存在且 `retry_count > 0` 的情况——这种时候不要重新调 LLM，而是直接读 state 里的 intent（已经在上轮被 `_relax_intent` 放宽过了）：

```python
def extract_intent(state: RecommendState) -> dict[str, Any]:
    # 关键：retry 模式下跳过 LLM，直接用上轮放宽后的 intent
    if state.get("retry_count", 0) > 0 and state.get("intent"):
        logger.info("[Node1] retry 模式，跳过 LLM，用已放宽的 intent")
        return {"intent": state["intent"], "used_mock": state.get("used_mock", False)}

    # ... 原逻辑（首次调用走 LLM/mock）...
```

### 3.2 缺口 G2：重试循环（Node1 LLM 抽 JSON 失败重试）

#### 3.2.1 设计思路

LLM 抽 JSON 失败有三种情况：
1. **网络/超时**：偶发，立即重试就好
2. **格式错误**（多了 ```json 标记、字段名漂移）：重试时带"上次输出 X，错误是 Y，请按 schema 重新输出"提示
3. **schema 必填字段缺失**：重试时强调缺失字段

LiteLLM 内置的 `num_retries` 只处理第 1 种（HTTP 重试），第 2/3 种要业务层自己实现。

#### 3.2.2 代码骨架

```python
# app/clients/llm.py 新增

def chat_structured_with_retry(
    messages: Iterable[dict[str, str]],
    output_schema: Type[T],
    max_retries: int = 2,
) -> tuple[T, list[dict]]:
    """带修正提示重试的结构化输出。返回 (结果, 全部尝试的 messages 记录)。"""
    all_messages = list(messages)
    last_exc: Exception | None = None

    for attempt in range(max_retries + 1):
        try:
            result = chat_structured(all_messages, output_schema)
            return result, all_messages
        except (RuntimeError, ValidationError) as exc:
            last_exc = exc
            if attempt == max_retries:
                break
            # 关键：累加修正提示，让 LLM 看到自己的错误
            correction = (
                f"你上次的输出有问题：{exc!s}\n"
                f"请严格按 JSON Schema 重新输出，不要包含任何 markdown 或多余字符。"
            )
            all_messages = all_messages + [{"role": "user", "content": correction}]
            logger.warning("[chat_structured] 第 %d 次失败，重试：%s", attempt + 1, exc)

    raise last_exc  # 重试耗尽，抛给上层走 fallback
```

#### 3.2.3 Node1 接入重试 + fallback 分层

```python
def extract_intent(state: RecommendState) -> dict[str, Any]:
    if state.get("retry_count", 0) > 0 and state.get("intent"):
        return {"intent": state["intent"], "used_mock": state.get("used_mock", False)}

    user_query = state.get("user_query") or ""
    city_override = state.get("city_code_override")

    if is_mock_mode():
        intent = _mock_extract_intent(user_query)
        return {"intent": intent, "used_mock": True}

    try:
        intent_obj, _ = chat_structured_with_retry(
            messages=[
                {"role": "system", "content": SYSTEM_NODE1},
                {"role": "user", "content": user_query},
            ],
            output_schema=IntentExtraction,
            max_retries=settings.max_retries,
        )
        intent = intent_obj.model_dump()
        used_mock = False
    except Exception as exc:
        # 三级降级：LLM 重试耗尽 → mock 兜底 → 记录告警
        logger.warning("[Node1] LLM 重试 %d 次仍失败，降级 mock：%s", settings.max_retries, exc)
        intent = _mock_extract_intent(user_query)
        used_mock = True

    if city_override:
        intent["city_name"] = city_override
    intent.setdefault("specialization_tags", [])
    return {"intent": intent, "used_mock": used_mock}
```

### 3.3 缺口 G3：Refine 循环（Node3 推荐理由质量门控）

#### 3.3.1 质量门控函数

```python
# app/graphs/recommend_coach.py 新增

_EMPTY_WORDS = ("很专业", "很棒", "非常好", "推荐", "优秀", "靠谱", "值得")

def _check_reason_quality(reason: str, candidates: list[dict]) -> tuple[bool, str]:
    """返回 (是否合格, 不合格原因)。合格标准：
       1. 长度 30~200 字
       2. 至少点出 2 位教练的差异化卖点（含教练名）
       3. 不含空泛词
    """
    if not reason or len(reason) < 30:
        return False, "理由过短（<30 字），缺乏信息量"
    if len(reason) > 200:
        return False, "理由过长（>200 字），需要精简"
    names_in_reason = sum(1 for c in candidates if c.get("name", "") in reason)
    if names_in_reason < 2 and len(candidates) >= 2:
        return False, "未点出至少 2 位教练的差异化"
    if any(w in reason for w in _EMPTY_WORDS) and names_in_reason < 2:
        return False, "含空泛词且无具体数据支撑"
    return True, ""
```

#### 3.3.2 Node3 改造（带 refine 循环）

```python
def generate_reason(state: RecommendState) -> dict[str, Any]:
    # ... 原 candidates 还原为 Pydantic ...

    retry = int(state.get("reason_retry_count", 0))
    last_feedback = state.get("reason_feedback", "")

    if used_mock:
        reason = _mock_generate_reason(...)
    else:
        top_coaches_brief = ...
        user_msg = f"用户目标：{intent.get('user_goal') or user_query}\n\n候选教练：\n{top_coaches_brief}"
        # 关键：retry 时累加修正提示
        if last_feedback:
            user_msg += f"\n\n你上次输出的理由不合格，原因：{last_feedback}。请重新生成。"
        try:
            reason = chat(messages=[{"role": "system", "content": SYSTEM_NODE3},
                                    {"role": "user", "content": user_msg}])
        except Exception as exc:
            logger.warning("[Node3] LLM 失败，回退 mock：%s", exc)
            reason = _mock_generate_reason(...)

    # 质量门控
    ok, why = _check_reason_quality(reason, candidates_dicts)
    if not ok and retry < settings.max_retries:
        logger.info("[Node3] 理由不合格（%s），回 Node3 重写（retry=%d）", why, retry)
        return {
            "reason_retry_count": retry + 1,
            "reason_feedback": why,
            "branch": "self_refine",   # 路由回自己
            # 注意：candidates / intent / matched_course 都已在 state 中，不重算
        }

    # 合格或重试耗尽 → 组装最终结果
    if not ok:
        logger.warning("[Node3] refine %d 次仍不合格，使用当前版本", retry)
        reason = _mock_generate_reason(...)  # 兜底用 mock 模板

    result_obj = RecommendResult(...)
    return {"result": result_obj.model_dump(), "used_mock": used_mock}
```

#### 3.3.3 路由接入

```python
_builder.add_conditional_edges(
    "generate_reason",
    ConditionalRouter(
        state_field="branch",
        mapping={
            "self_refine": "generate_reason",  # 回自己重写
            "done": END,
        },
        default="done",
    ).route,
)
```

### 3.4 缺口 G4：HITL（人工介入预留点）

#### 3.4.1 设计思路

教练推荐**本身不需要人工审核**——它是纯算法决策，自动化即可。但为了：
1. 给后续证书审核 Agent 跑通 `interrupt + Checkpointer` 基础设施
2. 应对"高价值订单人工确认"潜在场景（如月销 > 50 单的 VIP 客户推荐金牌教练时，可加一个运营复核点）

预留一个**可选启用**的 HITL 节点。默认关闭，配置开关 `HITL_ENABLED=false`。

#### 3.4.2 LangGraph interrupt 用法

```python
# app/graphs/recommend_coach.py 新增

from langgraph.types import interrupt, Command

def hitl_checkpoint(state: RecommendState) -> dict[str, Any]:
    """HITL 检查点：暂停等待人工确认推荐结果。
    默认不启用；启用后通过 graph.invoke(..., config={"configurable": {"thread_id": ...}})
    配合 interrupt + Command(resume=...) 完成。
    """
    candidates = state.get("candidates") or []
    if not candidates:
        return {"branch": "done"}

    # interrupt 会暂停 Graph，state 持久化到 Checkpointer
    # 人工可通过外部 API 调用 Command(resume={"approved": True/False}) 恢复
    human_decision = interrupt({
        "prompt": "请确认是否启用这批教练推荐",
        "candidates": [c.get("name") for c in candidates[:3]],
        "user_query": state.get("user_query"),
    })

    if not human_decision.get("approved"):
        # 人工拒绝 → 回 Node1 重新生成（带 feedback）
        return {
            "branch": "relax",
            "retry_count": int(state.get("retry_count", 0)) + 1,
            "reason_feedback": "人工拒绝了这批候选",
        }
    return {"branch": "done"}
```

#### 3.4.3 Graph 接入（条件启用）

```python
if settings.hitl_enabled:
    _builder.add_node("hitl_checkpoint", hitl_checkpoint)
    _builder.add_edge("generate_reason", "hitl_checkpoint")
    _builder.add_conditional_edges(
        "hitl_checkpoint",
        ConditionalRouter(
            state_field="branch",
            mapping={"relax": "extract_intent", "done": END},
            default="done",
        ).route,
    )
else:
    _builder.add_edge("generate_reason", END)
```

#### 3.4.4 外部 API 暴露 resume 端点

```python
# app/main.py 新增
from langgraph.types import Command

@app.post("/ai/recommend-coach/{thread_id}/resume", tags=["AI"])
def resume_recommend(thread_id: str, decision: dict):
    """人工恢复一个被 interrupt 的推荐流程。"""
    if not settings.hitl_enabled:
        raise HTTPException(501, "HITL 未启用")
    state_out = RECOMMEND_GRAPH.invoke(
        Command(resume=decision),
        config={"configurable": {"thread_id": thread_id}},
    )
    return RecommendResult.model_validate(state_out["result"])
```

---

## 4. Checkpointer 接入

### 4.1 为什么必须接

| 场景 | 无 Checkpointer | 有 Checkpointer |
|---|---|---|
| 进程崩了 | 整次推荐丢失，用户重试 | 从最后一个 checkpoint resume |
| 多副本部署 | thread_id 无意义，状态不一致 | thread_id 路由到 Redis，多副本共享 |
| HITL interrupt | 暂停后状态丢失，无法 resume | 暂停即持久化，等数小时也能恢复 |
| 调试 | 无法回放 | time travel 到任意 checkpoint |

### 4.2 RedisCheckpointer 接入

#### 4.2.1 依赖

```toml
# pyproject.toml 新增
"langgraph-checkpoint-redis==2.0.5",
```

#### 4.2.2 配置

```python
# app/config.py 新增
checkpoint_ttl: int = Field(default=3600, description="Checkpointer TTL（秒），教练推荐 1h 足够")
hitl_enabled: bool = Field(default=False, description="是否启用 HITL 人工介入（默认关）")
max_retries: int = Field(default=2, description="LLM 抽取/生成的最大重试次数")
```

```bash
# .env.example 新增
CHECKPOINT_TTL=3600
HITL_ENABLED=false
MAX_RETRIES=2
```

#### 4.2.3 main.py 改造

```python
# app/main.py 改造 RECOMMEND_GRAPH 的初始化
from langgraph.checkpoint.redis import RedisSaver
from langgraph.checkpoint.redis.aio import AsyncRedisSaver
from app.config import settings

def build_graph():
    builder = StateGraph(RecommendState)
    # ... add_node / add_edge / add_conditional_edges ...
    if settings.redis_url:
        # 生产：Redis Checkpointer
        saver = RedisSaver.from_conn_string(settings.redis_url, ttl=settings.checkpoint_ttl)
        return builder.compile(checkpointer=saver)
    # 开发：MemorySaver 兜底
    from langgraph.checkpoint.memory import MemorySaver
    return builder.compile(checkpointer=MemorySaver())

RECOMMEND_GRAPH = build_graph()
```

#### 4.2.4 thread_id 传递

每个 HTTP 请求分配一个 thread_id（基于 user_id + uuid4），让 Checkpointer 能隔离不同用户的推荐流程：

```python
# app/main.py recommend_coach 接口改造
import uuid

@app.post("/ai/recommend-coach", response_model=RecommendResult)
def recommend_coach(payload: RecommendCoachIn, x_user_id: str = Header(None)):
    thread_id = x_user_id or f"anon-{uuid.uuid4()}"
    state_in = {...}
    try:
        state_out = RECOMMEND_GRAPH.invoke(
            state_in,
            config={"configurable": {"thread_id": thread_id}},
        )
    except Exception as exc:
        ...
```

---

## 5. 改造后的完整拓扑

### 5.1 新拓扑图

```
START
  │
  ▼
┌─────────────────────┐
│ extract_intent      │ ◄──────────┐
│ (LLM + 重试 + mock) │            │
└─────────────────────┘            │
  │                                │  relax（放宽条件）
  ▼                                │  （retry < MAX）
┌─────────────────────┐            │
│ retrieve_and_rank   │            │
│ (SQL + 打分)        │────────────┘
└─────────────────────┘
  │
  │ branch:
  │   to_reason ──────► (继续往下)
  │   force_mock ─────► (用 mock 教练后往下)
  │   relax ─────────► (回 Node1)
  ▼
┌─────────────────────┐
│ generate_reason     │ ◄──────────┐
│ (LLM + 质量门控)    │            │
└─────────────────────┘            │
  │                                │  self_refine
  │ branch:                        │  （retry < MAX）
  │   self_refine ────► (回自己重写)
  │   done ───────────► (继续往下)
  ▼
┌─────────────────────┐
│ hitl_checkpoint     │  (可选，HITL_ENABLED=true 才有)
│ (interrupt)         │
└─────────────────────┘
  │
  │ branch:
  │   relax ─────────► (回 Node1)
  │   done ───────────► END
  ▼
 END
```

### 5.2 完整状态字段表

| 字段 | 类型 | 写入节点 | 用途 |
|---|---|---|---|
| user_query | str | 入参 | 用户原始查询 |
| city_code_override | str | 入参 | 城市覆盖 |
| top_n | int | 入参 | 返回数量 |
| intent | dict | extract_intent | 结构化意图 |
| candidates | list | retrieve_and_rank | Top N 候选 |
| matched_course | dict | retrieve_and_rank | 匹配课程 |
| over_budget | bool | retrieve_and_rank | 是否超预算 |
| used_mock | bool | extract_intent / generate_reason | 是否走了 mock |
| **retry_count** | int | retrieve_and_rank | Node1 放宽重试计数 |
| **relaxed_fields** | list[str] | retrieve_and_rank | 已放宽的字段名 |
| **reason_retry_count** | int | generate_reason | Node3 refine 重试计数 |
| **reason_feedback** | str | generate_reason | 上次不合格原因 |
| **branch** | str | retrieve_and_rank / generate_reason / hitl_checkpoint | 路由 key |
| result | dict | generate_reason | 最终结果 |

---

## 6. 落地步骤（按顺序执行）

| 步骤 | 文件 | 改动 | 估时 |
|---|---|---|---|
| 1 | `pyproject.toml` | 加 `langgraph-checkpoint-redis` 依赖 | 5min |
| 2 | `app/config.py` | 加 `checkpoint_ttl` / `hitl_enabled` / `max_retries` 三字段 | 5min |
| 3 | `.env.example` | 加对应环境变量 | 2min |
| 4 | `app/clients/llm.py` | 加 `chat_structured_with_retry` 函数 | 30min |
| 5 | `app/graphs/recommend_coach.py` | (1) 加 `_relax_intent` / `_check_reason_quality` / `hitl_checkpoint` (2) 改 `extract_intent` 识别 retry (3) 改 `retrieve_and_rank` 加分支 (4) 改 `generate_reason` 加门控 (5) 改 Graph 编译接入 ConditionalRouter + Checkpointer | 2h |
| 6 | `app/main.py` | 加 `build_graph()` 函数 + thread_id 传递 + `/resume` 端点 | 30min |
| 7 | `tests/test_recommend.py` | 加 4 个 case 覆盖 4 个缺口 | 1h |
| 8 | 跑 mock 模式全量回归 | `AI_MOCK=1 python -m tests.test_recommend` | 5min |

---

## 7. 验收标准

### 7.1 功能验收（必须全过）

新增 4 个测试 case，覆盖 4 个缺口：

| Case | 触发方式 | 期望行为 |
|---|---|---|
| **C1 重试** | mock 一个总是返回非法 JSON 的 LLM 客户端 | 重试 2 次后 fallback mock，日志含"重试 N 次" |
| **C2 条件分支** | 输入"西藏拉萨产后恢复"（mock 库无此城市教练） | 回 Node1 放宽 city → 仍空 → 放宽 district → 仍空 → 用 mock 教练 |
| **C3 Refine** | 注入一个返回 300 字理由的 LLM | refine 重写后 ≤200 字 |
| **C4 HITL** | `HITL_ENABLED=true` 调用，再调 `/resume` | interrupt 暂停 → resume 后返回结果 |

### 7.2 兼容性验收（不能回归）

原 [tests/test_recommend.py](../tests/test_recommend.py) 的 4 条 case（产后恢复+预算+时段 / 减脂+金牌 / 增肌+便宜 / 纯开放目标）**必须仍全过**。

### 7.3 性能验收

- mock 模式单次 invoke < 200ms（含 Checkpointer 写入）
- LLM 模式单次 invoke < 8s（含 1 次 LLM 调用 + Checkpointer 写入）

### 7.4 可观测验收

- 日志含 `[Node1] retry 模式` / `[Node2] 空候选，回 Node1 xxx` / `[Node3] 理由不合格（xxx）` 三类标记
- 每次路由决策有 INFO 日志

---

## 8. 关键设计决策回顾

| 决策点 | 选项 | 选择 | 理由 |
|---|---|---|---|
| 分支实现方式 | if-else / ConditionalRouter | ConditionalRouter | 拓扑显式声明，可观测 |
| 重试触发 | LiteLLM 内置 / 业务层 | 业务层 | 内置只处理 HTTP，不处理格式错误 |
| Refine 触发 | LLM 自评 / 规则门控 | 规则门控 | 节省 LLM 调用，规则明确 |
| HITL 启用方式 | 默认开 / 默认关 | 默认关 | 教练推荐本身不需要，开关控制 |
| Checkpointer 介质 | Memory / Redis / Postgres | Redis（生产）+ Memory（开发） | Redis 多副本共享 + TTL 自动清理 |
| thread_id 来源 | UUID / user_id | user_id（默认） + UUID（匿名） | 同用户多次推荐状态可关联 |
| 放宽策略 | 一次性全放宽 / 按序逐个 | 按序逐个 | 保留尽量多硬过滤，提升精度 |

---

## 9. 后续衔接

| 后续文档 | 与 #03 的关系 |
|---|---|
| #04 RAG 混合检索 | 替换 `retrieve_and_rank` 的实现，但保留本文的 `branch`/`retry_count`/`relaxed_fields` 状态字段 |
| #05 商业化加固 | 把 `RECOMMEND_GRAPH.invoke` 改为 `ainvoke`，Checkpointer 用 `AsyncRedisSaver` |
| #06 Harness 工程 | Eval 集会针对本文 4 个缺口新增 case，metric 加"重试次数分布" |
| #07 MCP 工具层 | `retrieve_and_rank` 内部的 SQL 调用改为 MCP client，但路由控制不变 |
| #08 证书审核 Agent | 复用本文的 `interrupt + Checkpointer + ConditionalRouter` 基础设施 |

---

## 10. 学习要点小结

读完这份文档，你应该掌握：

1. **Graph 拓扑应当显式声明**，不要藏在节点函数里 → 用 `add_conditional_edges + ConditionalRouter`
2. **重试与 fallback 分层**：HTTP 重试（LiteLLM 内置）→ 业务重试（带修正提示）→ mock 兜底，三层职责清晰
3. **能用规则就不用 LLM**：质量门控用 Python 函数，不用 LLM 自评
4. **基础设施先于业务跑通**：HITL 在不需要的场景先预留，等真用时不慌
5. **状态字段设计是 Loop 工程的核心**：`retry_count` / `relaxed_fields` / `branch` 三个字段决定了循环怎么走

回复「开始 #04」推进 RAG 混合检索升级，或先就 #03 某节展开讨论。
