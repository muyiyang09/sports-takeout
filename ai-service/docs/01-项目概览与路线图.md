# AI 微服务开发文档 · #01 现状总览与路线图

> 版本：v1.1 · 2026-08-27
> **文档分类**：项目概述（README） · **强制性**：入口参考 · **用途**：对外交付入口、项目快速理解
> 定位：本系列文档面向工程团队（含后端/AI/运维），记录 `ai-service` 从「能跑的 Demo」演进到「可商用 AI 微服务」的完整落地路径。
> 目标读者：项目维护者、二次开发者、商用买家评估方

---

## 0. 文档系列说明

本系列共 **12 份**，按「能力地图 → 分项落地 → 面试输出」三层组织。**前 8 份为工程落地，后 4 份为面试/交付辅助**：

| 编号 | 文档 | 主题 | 状态 |
|---|---|---|---|
| #01 | [现状总览与路线图](./01-项目概览与路线图.md) | 功能现状、落地清单、剩余路线 | ✅ 本文档 |
| #02 | [Agent 工程能力地图](./02-Agent工程能力地图.md) | 生产级 Agent 能力全景 + 学习路径 | ✅ 已落地 |
| #03 | [Loop 工程](./03-循环工程.md) | 条件分支、重试、HITL、refine 循环 | ✅ 已落地 |
| #04 | [RAG 混合检索](./04-RAG混合检索.md) | BM25 + Milvus + RRF + Rerank | ✅ 已落地 |
| #05 | [商业化加固](./05-商业化加固.md) | 异步化、连接池、限流熔断、可观测、容器化 | ✅ 已落地 |
| #06 | [Harness 工程与评估](./06-Harness工程与评估.md) | Eval 集、Metric、Trace、Prompt 管理 | ✅ 已落地 |
| #07 | [MCP 工具层](./07-MCP工具层.md) | MCP Server/Client、跨语言工具复用 | ✅ 已落地（默认关） |
| #08 | [多 Agent 实现](./08-多Agent实现.md) | 评价摘要 + 证书审核 Agent | ✅ 已落地 |
| #09 | [Agent 面试题集](./09-Agent面试题集.md) | 题库 + 自学清单 | ✅ 交付辅助 |
| #10 | [上线检查清单](./10-上线检查清单.md) | Go-Live Checklist | ✅ 交付辅助 |
| #11 | [Agent 场景面经](./11-Agent场景面经.md) | 「讲项目」标准答案 | ✅ 交付辅助 |
| #12 | [面试速记话术](./12-面试速记话术.md) | 背诵版话术 | ✅ 交付辅助 |

> 写作约定：落地类文档以「现状 → 缺口 → 落地方案 → 验收标准」四段式展开；面试类文档以「题目 → 答案 → 话术」展开。

---

## 1. 当前功能现状

### 1.1 服务边界

| 项 | 值 |
|---|---|
| 服务名 | `sports-takeout-ai` |
| 角色 | Spring Boot 主后端旁挂的独立 AI 微服务，**只读** sports_takeout 库 |
| 端口 | `18000`（MCP 预留 `18001`） |
| HTTP 框架 | FastAPI 0.115.6 + Uvicorn 0.32.1 |
| 部署形态 | Dockerfile 多副本（`--workers 2`）+ docker-compose 编排，已进主编排 |
| Agent 框架 | LangGraph 1.1.4（StateGraph + Checkpointer + interrupt） |
| LLM 路由 | LiteLLM 1.55.3（DeepSeek / Qwen / OpenAI） |
| 向量库 | Milvus（生产唯一；pymilvus 可选重依赖，未装时降级 BM25 单路） |

**对外接口清单：**

| Method | Path | 说明 |
|---|---|---|
| GET | `/healthz` | Liveness：进程存活（不查依赖） |
| GET | `/readyz` | Readiness：DB / Redis / LLM 探活 |
| GET | `/metrics` | Prometheus 指标导出 |
| POST | `/v1/ai/recommend-coach` | 教练智能推荐（Agent 1） |
| POST | `/v1/ai/feedback` | 用户反馈回流（在线 Eval 集） |
| POST | `/v1/ai/review-summary` | 评价摘要（Agent 2） |
| POST | `/v1/ai/cert-review` | 证书审核（Agent 3，HITL 开启时返回 pending） |
| POST | `/v1/ai/cert-review/{thread_id}/resume` | HITL 人工确认（approve / reject） |
| POST | `/v1/ai/cert-review/{thread_id}/cancel` | HITL 取消审核单 |
| POST | `/v1/ai/chat` | 统一入口（Supervisor 路由） |

### 1.2 三个 Agent + 一个路由

| Agent | 图文件 | 职责 | 关键能力 |
|---|---|---|---|
| `recommend_coach` | [graphs/recommend_coach.py](../app/graphs/recommend_coach.py) | 自然语言 → Top N 教练 + 理由 | 3 节点 DAG（意图抽取 → 混合检索 + 5 维打分 → 理由生成）；BM25+Milvus 混合召回 |
| `review_summary` | [graphs/review_summary.py](../app/graphs/review_summary.py) | 批量评价 → 优缺点 + 标签 | 多评价 reduce 摘要 |
| `cert_review` | [graphs/cert_review.py](../app/graphs/cert_review.py) | 证书 OCR → 核验 → 风险评估 | 工具循环 + HITL `interrupt` + 管理员 resume |
| `supervisor` | [graphs/supervisor.py](../app/graphs/supervisor.py) | `/v1/ai/chat` 统一路由 | LLM 判定意图 → 派发对应 Agent（默认关，可直连子 Agent） |

**推荐 Agent 拓扑（3 节点，Node2 空结果走 conditional 回退）：**

```
START → Node1 意图抽取(LLM) → Node2 检索+打分(规则+混合检索) → Node3 理由生成(LLM) → END
                                    └── 空候选 → 回 Node1 放宽条件重抽（Loop）
```

### 1.3 已落地的工程化能力（家底清单）

| 维度 | 落地内容 | 关键文件 |
|---|---|---|
| 混合检索 | BM25（jieba + rank_bm25）稀疏召回 + Milvus 稠密召回 + RRF 融合 + 可选 Cross-Encoder 精排 | `clients/bm25.py` / `vectorstore.py` / `hybrid.py` / `reranker.py` / `embedding.py` |
| Loop 工程 | 条件分支、LLM 抽取重试、空结果 refine 回退、HITL interrupt | `graphs/base.py` / `graphs/recommend_coach.py` / `graphs/cert_review.py` |
| 多 Agent | 3 Agent + Supervisor 路由 | `graphs/` |
| Checkpointer | RedisSaver（生产，多副本共享 + TTL）+ memory（开发）+ DB 灾备回填 | `core/checkpoint.py` / `core/checkpoint_redis_db.py` / `core/session_store.py` |
| HITL | 证书审核 interrupt 暂停 → 管理员 resume，状态机防重复处理 | `core/hitl_state.py` |
| 商业化加固 | 全链路异步、连接池、三级限流、Token 预算、LLM 熔断、结果缓存、审计、优雅停机 | `middleware/` / `core/audit.py` / `core/metrics.py` / `core/safety.py` |
| 可观测 | 结构化 JSON 日志 + request_id、Prometheus 指标、`/readyz`/`/healthz` 分离 | `core/logging.py` / `core/metrics.py` |
| Harness | Eval 集 + Metric + Judge、Prompt YAML 化 + 版本化、Trace 骨架 | `eval/` / `prompts/` / `clients/trace.py` |
| MCP 工具层 | MCP Server/Client（默认关，单进程直连够用） | `mcp/` |
| 安全 | 提示词注入检测、错误码不泄露内部栈、SQL 参数化 | `core/safety.py` / `core/exceptions.py` |
| 容器化 | 多阶段构建、非 root 运行、健康探针、优雅停机 | `Dockerfile` / 项目根 `docker-compose.yml` |

### 1.4 技术栈

| 类别 | 选型 | 版本 | 说明 |
|---|---|---|---|
| Agent 框架 | LangGraph | 1.1.4 | StateGraph + add_messages + interrupt |
| 消息核心 | langchain-core | ≥1.0 | BaseMessage + add_messages reducer |
| Checkpointer | langgraph-checkpoint-redis | 0.5.2 | RedisSaver（生产） |
| LLM 路由 | LiteLLM | 1.55.3 | 多供应商统一接口 |
| 数据校验 | Pydantic | 2.10.4 | 结构化输出 + 字段归一化 |
| 配置 | pydantic-settings | 2.6.1 | 强类型 .env |
| HTTP | FastAPI / Uvicorn | 0.115.6 / 0.32.1 | 异步 + OpenAPI |
| DB | SQLAlchemy + pymysql | 2.0.36 / 1.1.1 | 只读现有库 |
| Redis | redis | 7.4.1 | 缓存/限流/预算/Checkpointer |
| 稀疏检索 | rank-bm25 + jieba | 0.2.2 / 0.42.1 | BM25 + 中文分词 |
| 向量检索 | pymilvus（可选） | ≥2.4.0 | MilvusClient + HNSW/COSINE |
| Embedding | FlagEmbedding bge-m3（可选） | — | 重依赖，惰性加载 |
| Python | ≥3.11 | — | `list[str]` 等新语法 |

### 1.5 轻量降级设计（关键架构决策）

向量/重排重模型（bge-m3 / bge-reranker，GB 级）在国内难下载，故采用**逐级降级**策略，任何一路缺失都不抛异常打断主链路：

```
3 路（BM25 + 向量 + 重排）→ 2 路（BM25 + 向量）→ 1 路（BM25 单路 min-max 归一化）→ 0 路（退回 #03 子串匹配）
```

- Milvus 不可达 / pymilvus 未装 → 向量召回返回空，自动走 BM25 单路。
- Embedding 模型未装 → `embed()` 返回空数组，`upsert_coaches` 返回 0，不阻塞启动。
- 重排依赖缺失 → no-op 原样返回。
- 所有「保护组件」（Redis 限流/预算）故障 → fail-open，放过流量而非全拦。

---

## 2. 商业化落地对比（从差距到现状）

> 原 v1.0 本文档列出的商业化差距，现逐项标注落地状态。**已全部消除或降级为「可选重依赖」**。

| 维度 | 原差距 | 现状 |
|---|---|---|
| API 入口同步阻塞 | 同步 `def` 阻塞 event loop | ✅ 全链路 async + `ainvoke` + `acompletion` |
| MySQL 连接池默认 5 | 峰值并发打满 | ✅ `pool_size=20` + `max_overflow=10` + `pool_pre_ping` |
| Redis 缓存未用 | 相同 query 每次打 LLM | ✅ 24h 结果缓存 + cache_key |
| 无限流 | 恶意刷 | ✅ 三级限流（全局/IP/用户）+ Token 预算 |
| 无熔断 | LLM 连续失败拖垮 | ✅ LLM 熔断器（半开试探） |
| 部署形态单实例 | 无容器化 | ✅ Dockerfile + 多副本 + compose 编排 |
| 健康探针浅 | 不查依赖 | ✅ `/healthz`(liveness) 与 `/readyz`(readiness) 分离 |
| 无优雅停机 | SIGTERM 丢请求 | ✅ lifespan + `--timeout-graceful-shutdown` |
| 无监控 | 看不见瓶颈 | ✅ `/metrics` + 结构化日志 + request_id |
| 目录分层混乱 | main.py 混编排 | ✅ clients/graphs/middleware/core/eval/mcp/tools 分层 |
| 错误码缺失 | 通用 500 | ✅ 错误码 + 全局异常中间件 + 不泄露栈 |
| 测试覆盖不足 | 1 个 smoke test | ✅ 10 个测试模块（hybrid/hardening/loop/eval/tools/checkpoint/safety/metrics/agents/recommend） |
| API 无版本 | 破坏性变更无退路 | ✅ `/v1/` 前缀 |

---

## 3. 剩余路线图（重排后）

已落地 Phase 1–5（Loop / 加固 / Harness / MCP / 多 Agent），剩余项按「ROI + 依赖」重排：

### 🟠 P0 · 向量路真跑起来（依赖环境，非代码缺口）
- **装重依赖**：`cd ai-service && pip install -e ".[vector-milvus]"`（pymilvus）+ bge-m3 embedding 模型。
- **起 Milvus**：`docker-compose up -d milvus`（已进主编排，含 etcd + minio + milvus + attu）。
- **验收**：`POST /v1/ai/recommend-coach` 日志出现 `[VectorStore] 召回 N 条`；`upsert_coaches` 返回 >0。
- 代码侧已就绪，缺的只是**有 GPU / 可访问 HuggingFace 的部署环境**。

### 🟡 P1 · 在线 Eval 回流闭环
- `feedback` 端点已把用户点赞/点踩/下单写入 `ai_eval_online` 表（在线 ground truth 集）。
- 待补：定时任务把在线反馈回流成 Eval 集 → 触发离线 metric 回归 → 依据结果调 prompt/阈值。

### 🟡 P2 · Cross-Encoder 精排上线
- `reranker.py` + 配置开关 `RERANKER_ENABLED` 已就绪，默认关。
- 上线前提：下载 bge-reranker-v2-m3 重模型，改 `.env` 打开开关。

### 🟢 P3 · 可选增强
- MCP 工具层默认关（单进程直连够用），需跨语言/外部 LLM 共享工具时再开 `MCP_ENABLED`。
- Supervisor 统一入口默认关，直接调子 Agent 更省一层 LLM 路由。

---

## 4. 下一步行动

1. **P0 向量路**：在有 GPU / 可下载模型的环境跑通 Milvus 向量召回（§3 P0），这是当前唯一「改了代码但还没真跑过」的链路。
2. **P1 Eval 回流**：把 `feedback` 在线数据接进离线 metric 回归，形成持续优化闭环。
3. 其余均为可选增强，按业务需要逐步开启，不阻塞交付。

> 本文档随代码演进持续更新；落地类细节以 #02~#08 为准，面试输出以 #09~#12 为准。
