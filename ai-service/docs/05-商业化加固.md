# AI 微服务开发文档 · #05 商业化加固

> 版本：v1.0 · 2026-08-26
> **文档分类**：技术方案（Blueprint + Guideline） · **强制性**：建议遵循 · **用途**：把 ai-service 从「能跑的 Demo」加固到「最小可商用」
> 前置阅读：[#01 §2 商业化差距分析](./01-项目概览与路线图.md) · [#02 §11 部署与运维](./02-Agent工程能力地图.md)
> 涉及文件：`app/main.py` / `app/config.py` / `app/middleware/`（新增）/ `app/core/`（新增）/ `Dockerfile`（新增）/ `docker-compose.yml`（项目根）

---

## 0. 文档说明

### 0.1 目标

把 ai-service 从「最小可跑」加固到「最小可商用」，达到 6 项硬指标：

1. ✅ 异步化全链路（不被同步调用阻塞 event loop）
2. ✅ 容器化 + 多副本部署（高可用）
3. ✅ Trace + Metrics（看得见在跑什么）
4. ✅ Token Budget + Audit Log（成本可控 + 合规）
5. ✅ 限流 + 熔断（不被打挂）
6. ✅ API 版本前缀 + 结构化日志（工程协作）

### 0.2 范围与边界

| 项 | 是否改动 |
|---|---|
| 对外 HTTP 契约 | ⚠️ 加 `/v1` 版本前缀（破坏性，需协调前端） |
| Graph 内部逻辑 | ❌ 不动（沿用 #03 / #04 改造） |
| 部署形态 | ✅ 容器化 + 多副本 |
| 配置分层 | ✅ dev/test/prod 多环境 |

---

## 1. 现状回顾

### 1.1 当前同步链路问题

```python
# app/main.py 当前
def recommend_coach(payload: RecommendCoachIn) -> RecommendResult:
    # ↑ 同步函数，每个请求占用 worker 线程池一个线程
    state_out = RECOMMEND_GRAPH.invoke(state_in)  # ← 同步 invoke
```

**问题**：FastAPI 是 ASGI 异步框架，但用 `def` 定义的端点会被丢到线程池执行。线程池默认 40 个线程，第 41 个并发请求会排队等。LLM 调用 60s 超时，40 个并发能阻塞所有线程，服务假死。

### 1.2 高可用缺口清单

| 项 | 现状 | 期望 |
|---|---|---|
| 部署 | `python -m app.main` | Docker + K8s 多副本 |
| 健康探针 | `/healthz` 浅探活 | `/healthz` (liveness) + `/readyz` (readiness 含 DB/LLM/Redis 探活) |
| 优雅停机 | 无 | SIGTERM → 拒新请求 → 等在飞 → 退出 |
| 熔断降级 | 节点级 fallback mock | 全局熔断器（连续 N 次失败 → 短路 → 走 mock） |
| 限流 | 无 | 每用户/IP/全局三级限流 |
| Token 预算 | 无 | 单用户单日上限 + 全局日上限 |
| 审计日志 | 无 | 所有 LLM/Tool 调用入 `ai_audit_log` 表 |
| 监控 | 无 | Prometheus metrics + Grafana |

---

## 2. 设计思路

### 2.1 异步化为什么是商业化的第一步

| 同步模型 | 异步模型 |
|---|---|
| 1000 并发需要 1000 线程 | 1 进程 1 事件循环扛 10000 并发 |
| 线程切换开销大 | 协程切换纳秒级 |
| 阻塞调用 = 服务假死 | I/O 等待时让出 CPU 给其他请求 |
| LiteLLM 调用占满线程池 | acompletion 立即让出 |

**关键认知**：LLM 调用本质是 I/O 密集型（等远程 API 返回），不是 CPU 密集型。I/O 密集型场景异步收益最大。

### 2.2 限流 vs 熔断 vs 降级

三个概念常被混淆：

| 机制 | 触发条件 | 目的 | 实现位置 |
|---|---|---|---|
| **限流** | QPS 超阈值 | 保护下游 | 入口（middleware） |
| **熔断** | 错误率超阈值 | 快速失败，避免雪崩 | 调用方（client 包装） |
| **降级** | 主链路失败 | 返回兜底结果 | 业务节点 |
| **重试** | 偶发失败 | 自动恢复 | client 包装（含退避） |

顺序：**限流在前 → 熔断在中 → 降级在后**。限流挡住恶意流量，熔断快速失败不传染，降级保证用户拿到结果（哪怕是 mock）。

### 2.3 Token Budget 为什么必须

LLM 成本是变量，恶意用户/被劫持账号能瞬间烧光预算。

| 维度 | 阈值 | 触发动作 |
|---|---|---|
| 单用户单次请求 | 10K token | 拒绝（防长 prompt 攻击） |
| 单用户单日 | 100K token | 拒绝并告警 |
| 全局单日 | 1M token | 告警，开启 mock 模式 |
| 全局单月 | 30M token | 告警，联系商务 |

### 2.4 为什么 API 加版本前缀

```
当前：POST /ai/recommend-coach
改后：POST /v1/ai/recommend-coach
```

破坏性变更（schema 字段调整/语义变化）必须走 `/v2`，老版本保留至少 6 个月。商业项目客户最怕"接口突然变了"。

---

## 3. 落地方案

### 3.1 异步化全链路

#### 3.1.1 API 入口改 async

```python
# app/main.py 改造
from fastapi import Request

@app.post("/v1/ai/recommend-coach", response_model=RecommendResult)
async def recommend_coach(
    payload: RecommendCoachIn,
    request: Request,
    x_user_id: str = Header(None),
) -> RecommendResult:
    thread_id = x_user_id or f"anon-{uuid.uuid4()}"
    state_in = {...}

    # 关键：用 ainvoke 而非 invoke
    state_out = await RECOMMEND_GRAPH.ainvoke(
        state_in,
        config={"configurable": {"thread_id": thread_id}},
    )
    return RecommendResult.model_validate(state_out["result"])
```

#### 3.1.2 LLM 客户端改 async

```python
# app/clients/llm.py 已有 achat，但要包装 async chat_structured
async def achat_structured_with_retry(
    messages: Iterable[dict[str, str]],
    output_schema: Type[T],
    max_retries: int = 2,
) -> tuple[T, list[dict]]:
    all_messages = list(messages)
    last_exc = None
    for attempt in range(max_retries + 1):
        try:
            # 用 acompletion 而非 completion
            return await _achat_structured_async(all_messages, output_schema), all_messages
        except (RuntimeError, ValidationError) as exc:
            last_exc = exc
            if attempt == max_retries:
                break
            all_messages = all_messages + [{"role": "user", "content": f"...{exc}..."}]
    raise last_exc


async def _achat_structured_async(messages, schema):
    """async 版本的 chat_structured，用 acompletion"""
    schema_json = schema.model_json_schema()
    system_with_schema = "..."
    msgs = [{"role": "system", "content": system_with_schema}, *list(messages)]
    raw_text = await achat(msgs)
    parsed = json.loads(raw_text)
    data = normalize_for_pydantic(parsed, schema)
    return schema.model_validate(data)
```

#### 3.1.3 DB 客户端改 async

```python
# app/clients/db.py 改造
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession

_async_engine = None

def get_async_engine():
    global _async_engine
    if _async_engine is None:
        # pymysql 不支持 async，换成 aiomysql
        dsn = settings.mysql_dsn.replace("pymysql", "aiomysql")
        _async_engine = create_async_engine(
            dsn, pool_pre_ping=True, pool_recycle=3600,
            pool_size=settings.mysql_pool_size,
            max_overflow=settings.mysql_max_overflow,
        )
    return _async_engine


async def afetch_all(sql: str, params: dict = None) -> list[dict]:
    async with get_async_engine().connect() as conn:
        rows = await conn.execute(text(sql), params or {})
        cols = list(rows.keys())
        return [dict(zip(cols, row)) for row in rows]
```

#### 3.1.4 Graph 节点改 async

```python
# app/graphs/recommend_coach.py 改造
async def extract_intent(state: RecommendState) -> dict[str, Any]:
    if state.get("retry_count", 0) > 0 and state.get("intent"):
        return {"intent": state["intent"], "used_mock": state.get("used_mock", False)}

    if is_mock_mode():
        return {"intent": _mock_extract_intent(state.get("user_query", "")), "used_mock": True}

    intent_obj, _ = await achat_structured_with_retry(...)
    return {"intent": intent_obj.model_dump(), "used_mock": False}
```

### 3.2 连接池参数化

```python
# app/config.py 新增
mysql_pool_size: int = Field(default=20, description="MySQL 连接池大小")
mysql_max_overflow: int = Field(default=10, description="连接池溢出上限")
mysql_pool_timeout: int = Field(default=30, description="获取连接超时")
llm_concurrency: int = Field(default=50, description="LLM 并发上限（信号量）")
```

### 3.3 Redis 缓存层

```python
# app/clients/cache.py（新增）
"""Redis 缓存：高频相同 query 缓存 24h，节省 LLM 调用"""
import json
import hashlib
import redis.asyncio as redis
from app.config import settings

_pool = redis.ConnectionPool.from_url(settings.redis_url)

async def get_cache(key: str) -> dict | None:
    r = redis.Redis(connection_pool=_pool)
    val = await r.get(key)
    return json.loads(val) if val else None

async def set_cache(key: str, value: dict, ttl: int = 86400):
    r = redis.Redis(connection_pool=_pool)
    await r.setex(key, ttl, json.dumps(value, ensure_ascii=False, default=str))

def make_cache_key(user_query: str, top_n: int, city_override: str = None) -> str:
    raw = f"{user_query}|{top_n}|{city_override or ''}"
    return f"recommend:{hashlib.md5(raw.encode()).hexdigest()}"
```

API 入口加缓存：

```python
@app.post("/v1/ai/recommend-coach", response_model=RecommendResult)
async def recommend_coach(payload: RecommendCoachIn, request: Request, ...):
    # 缓存命中检查
    cache_key = make_cache_key(payload.user_query, payload.top_n, payload.city_code_override)
    cached = await get_cache(cache_key)
    if cached:
        logger.info("[Cache] 命中，跳过 LLM")
        return RecommendResult.model_validate(cached)

    # ... 走 Graph ...
    result = RecommendResult.model_validate(state_out["result"])
    # 写缓存
    await set_cache(cache_key, result.model_dump())
    return result
```

### 3.4 限流中间件

```python
# app/middleware/rate_limit.py（新增）
"""三级限流：全局 + IP + 用户"""
import time
import redis.asyncio as redis
from fastapi import Request, HTTPException
from starlette.middleware.base import BaseHTTPMiddleware

class RateLimitMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request: Request, call_next):
        # 跳过非业务路径
        if not request.url.path.startswith("/v1/ai/"):
            return await call_next(request)

        user_id = request.headers.get("x-user-id", "anon")
        ip = request.client.host
        now = int(time.time())

        r = redis.Redis(connection_pool=_pool)
        # 三级检查
        for key, limit, window in [
            (f"rl:global:{now // 60}", settings.rl_global_per_min, 60),
            (f"rl:ip:{ip}:{now // 60}", settings.rl_ip_per_min, 60),
            (f"rl:user:{user_id}:{now // 60}", settings.rl_user_per_min, 60),
        ]:
            cnt = await r.incr(key)
            await r.expire(key, window)
            if cnt > limit:
                raise HTTPException(429, f"请求过频：{key.split(':')[0]}")

        return await call_next(request)
```

### 3.5 LLM 熔断器

```python
# app/clients/circuit_breaker.py（新增）
"""简单熔断器：连续 N 次失败 → 开路 → 一段时间后半开试探"""
import time
from enum import Enum
from typing import Callable, Awaitable

class CircuitState(Enum):
    CLOSED = "closed"      # 正常
    OPEN = "open"          # 熔断
    HALF_OPEN = "half_open"  # 半开试探

class CircuitBreaker:
    def __init__(self, fail_threshold=5, reset_timeout=60):
        self.fail_count = 0
        self.state = CircuitState.CLOSED
        self.last_fail_time = 0
        self.fail_threshold = fail_threshold
        self.reset_timeout = reset_timeout

    async def call(self, fn: Callable[..., Awaitable], *args, **kwargs):
        if self.state == CircuitState.OPEN:
            if time.time() - self.last_fail_time > self.reset_timeout:
                self.state = CircuitState.HALF_OPEN
            else:
                raise RuntimeError("CircuitBreaker OPEN")

        try:
            result = await fn(*args, **kwargs)
            if self.state == CircuitState.HALF_OPEN:
                self.state = CircuitState.CLOSED
                self.fail_count = 0
            return result
        except Exception as exc:
            self.fail_count += 1
            self.last_fail_time = time.time()
            if self.fail_count >= self.fail_threshold:
                self.state = CircuitState.OPEN
            raise

llm_breaker = CircuitBreaker(fail_threshold=5, reset_timeout=60)
```

### 3.6 Token 预算

```python
# app/middleware/token_budget.py（新增）
"""Token 预算控制：单用户单次/单日/全局单日三级"""
import tiktoken
import redis.asyncio as redis

_encoder = tiktoken.get_encoding("cl100k_base")

async def count_tokens(text: str) -> int:
    return len(_encoder.encode(text))

async def check_budget(user_id: str, input_tokens: int, r: redis.Redis) -> None:
    # 单次请求上限
    if input_tokens > settings.token_per_request_limit:
        raise HTTPException(400, f"输入超长：{input_tokens}>{settings.token_per_request_limit}")

    # 单用户单日
    daily_key = f"tok:user:{user_id}:{time.strftime('%Y%m%d')}"
    used = await r.incrby(daily_key, 0)
    if used > settings.token_user_daily_limit:
        raise HTTPException(429, "单日 token 预算耗尽")
    # 全局单日
    global_key = f"tok:global:{time.strftime('%Y%m%d')}"
    used_g = await r.incrby(global_key, 0)
    if used_g > settings.token_global_daily_limit:
        raise HTTPException(503, "全局 token 预算耗尽，已切换 mock 模式")
```

### 3.7 审计日志

```python
# app/core/audit.py（新增）
"""审计日志：所有 LLM/Tool 调用入 MySQL ai_audit_log 表"""
import time
import uuid
from app.clients.db import afetch_all  # 异步

async def log_audit(
    user_id: str,
    request_id: str,
    action: str,         # "llm_call" / "tool_call" / "graph_invoke"
    model: str = None,
    prompt: str = None,
    response: str = None,
    input_tokens: int = 0,
    output_tokens: int = 0,
    duration_ms: int = 0,
    success: bool = True,
    error: str = None,
):
    sql = """
    INSERT INTO ai_audit_log
    (id, user_id, request_id, action, model, prompt, response,
     input_tokens, output_tokens, duration_ms, success, error, created_at)
    VALUES (:id, :uid, :rid, :act, :model, :prompt, :resp,
            :it, :ot, :dur, :ok, :err, NOW())
    """
    await afetch_all(sql, {
        "id": str(uuid.uuid4()), "uid": user_id, "rid": request_id,
        "act": action, "model": model, "prompt": prompt[:1000] if prompt else None,
        "resp": response[:1000] if response else None,
        "it": input_tokens, "ot": output_tokens, "dur": duration_ms,
        "ok": success, "err": error,
    })
```

### 3.8 结构化日志 + request_id

```python
# app/core/logging.py（新增）
"""结构化 JSON 日志 + request_id 贯穿"""
import logging
import json
import sys
import uuid

class JsonFormatter(logging.Formatter):
    def format(self, record):
        log = {
            "ts": self.formatTime(record),
            "level": record.levelname,
            "logger": record.name,
            "msg": record.getMessage(),
            "request_id": getattr(record, "request_id", "-"),
            "user_id": getattr(record, "user_id", "-"),
        }
        if record.exc_info:
            log["exc"] = self.formatException(record.exc_info)
        return json.dumps(log, ensure_ascii=False)

# main.py 初始化
handler = logging.StreamHandler(sys.stdout)
handler.setFormatter(JsonFormatter())
logging.basicConfig(level=logging.INFO, handlers=[handler])

# 中间件注入 request_id
class RequestIdMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request, call_next):
        request_id = request.headers.get("x-request-id", str(uuid.uuid4()))
        request.state.request_id = request_id
        # 用 contextvar 让所有日志自动带 request_id
        _request_id_ctx.set(request_id)
        response = await call_next(request)
        response.headers["x-request-id"] = request_id
        return response
```

### 3.9 健康探针分离

```python
@app.get("/healthz", tags=["System"])
async def healthz():
    """Liveness：进程是否活着。永远返回 200，不查依赖。"""
    return {"ok": True}

@app.get("/readyz", tags=["System"])
async def readyz():
    """Readiness：是否准备好接流量。查 DB / Redis / LLM。"""
    checks = {}
    checks["db"] = await _check_db()
    checks["redis"] = await _check_redis()
    checks["llm"] = "mock" if is_mock_mode() else await _check_llm()
    ok = all(v in ("ok", "mock") for v in checks.values())
    return {"ok": ok, "checks": checks}
```

### 3.10 全局异常处理

```python
# app/core/exceptions.py（新增）
class AIError(Exception):
    code: str = "AI_INTERNAL"
    msg: str = "AI 服务内部错误"

class LLMTimeoutError(AIError):
    code = "AI_LLM_TIMEOUT"
    msg = "LLM 调用超时"

class TokenBudgetExceededError(AIError):
    code = "AI_TOKEN_BUDGET"
    msg = "Token 预算耗尽"

# main.py 注册全局异常处理
@app.exception_handler(AIError)
async def ai_error_handler(request, exc: AIError):
    logger.exception("AIError: %s", exc.code, extra={"request_id": getattr(request.state, "request_id", "-")})
    return JSONResponse(
        status_code=500,
        content={"code": exc.code, "msg": exc.msg, "request_id": request.state.request_id},
    )
```

### 3.11 Dockerfile

```dockerfile
# ai-service/Dockerfile
FROM python:3.11-slim AS builder
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential gcc && rm -rf /var/lib/apt/lists/*
COPY pyproject.toml ./
RUN pip install --user --no-cache-dir -e .

FROM python:3.11-slim
WORKDIR /app
# 非 root 用户
RUN useradd -m -u 1000 appuser
COPY --from=builder /root/.local /home/appuser/.local
COPY . .
ENV PATH=/home/appuser/.local/bin:$PATH
USER appuser
EXPOSE 18000
HEALTHCHECK --interval=30s --timeout=5s --retries=3 \
  CMD curl -f http://localhost:18000/healthz || exit 1
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "18000", "--workers", "4"]
```

### 3.12 docker-compose 加入 ai-service

```yaml
# docker-compose.yml 新增
ai-service:
  build: ./ai-service
  container_name: sports-ai
  environment:
    - LLM_MODEL=${LLM_MODEL:-deepseek/deepseek-chat}
    - LLM_API_KEY=${LLM_API_KEY}
    - MYSQL_HOST=mysql
    - REDIS_HOST=redis
    - SERVICE_ENV=prod
  ports:
    - "18000:18000"
  depends_on:
    mysql:
      condition: service_healthy
    redis:
      condition: service_started
  restart: unless-stopped
  deploy:
    replicas: 2  # 多副本
    resources:
      limits:
        memory: 2G
        cpus: "2.0"
```

### 3.13 优雅停机

```python
# app/main.py lifespan 改造
from signal import SIGTERM, SIGINT

@asynccontextmanager
async def lifespan(app: FastAPI):
    # 启动
    logger.info("启动...")
    # 预构建索引
    if settings.hybrid_retrieval_enabled:
        asyncio.create_task(_build_indices_async())

    stop_event = asyncio.Event()

    def _signal_handler():
        stop_event.set()

    loop = asyncio.get_event_loop()
    for sig in (SIGTERM, SIGINT):
        loop.add_signal_handler(sig, _signal_handler)

    yield

    # 优雅停机
    logger.info("收到停止信号，等待在飞请求...")
    # uvicorn --timeout-graceful-shutdown=30 已处理 worker 层
    # 这里只清理应用层资源
    await _close_async_engine()
    await _close_redis_pool()
    logger.info("停止完成")
```

---

## 4. 配置项汇总

```python
# app/config.py 新增所有商业化字段
# —— MySQL 连接池 ——
mysql_pool_size: int = 20
mysql_max_overflow: int = 10
mysql_pool_timeout: int = 30

# —— 限流 ——
rl_global_per_min: int = 100
rl_ip_per_min: int = 30
rl_user_per_min: int = 10

# —— Token 预算 ——
token_per_request_limit: int = 10000
token_user_daily_limit: int = 100000
token_global_daily_limit: int = 1000000

# —— 熔断 ——
cb_fail_threshold: int = 5
cb_reset_timeout: int = 60

# —— 缓存 ——
cache_ttl: int = 86400
cache_enabled: bool = True
```

---

## 5. 落地步骤

| 步骤 | 文件 | 改动 |
|---|---|---|
| 1 | `app/clients/db.py` | 加 async engine + afetch_all |
| 2 | `app/clients/llm.py` | 包装 achat_structured_with_retry |
| 3 | `app/clients/cache.py` | 新建 Redis 缓存层 |
| 4 | `app/clients/circuit_breaker.py` | 新建熔断器 |
| 5 | `app/middleware/rate_limit.py` | 新建限流中间件 |
| 6 | `app/middleware/token_budget.py` | 新建 token 预算中间件 |
| 7 | `app/core/audit.py` | 新建审计日志 |
| 8 | `app/core/logging.py` | 新建结构化日志 |
| 9 | `app/core/exceptions.py` | 新建错误码体系 |
| 10 | `app/main.py` | async 化 + 中间件挂载 + /v1 前缀 + /readyz + 优雅停机 |
| 11 | `app/graphs/recommend_coach.py` | 节点改 async |
| 12 | `Dockerfile` | 新建 |
| 13 | `docker-compose.yml` | 加 ai-service 服务 |
| 14 | `sql/ai_audit_log.sql` | 新建审计表 |

---

## 6. 验收标准

### 6.1 性能验收

| 指标 | 期望 |
|---|---|
| 单实例 1000 QPS（mock 模式） | p99 < 500ms |
| LLM 模式 50 并发 | p99 < 10s |
| 缓存命中 | < 50ms |
| 熔断触发后响应 | < 10ms（不调 LLM） |

### 6.2 高可用验收

| 场景 | 期望 |
|---|---|
| 杀掉 1 个副本 | 0 失败请求（流量切到其他副本） |
| LLM 持续失败 5 次 | 触发熔断，后续请求走 mock |
| 流量超 1000 QPS | 429 限流，不雪崩 |
| 用户超 token 预算 | 429 拒绝并告警 |
| SIGTERM | 30s 内优雅退出，0 失败请求 |

### 6.3 可观测验收

- 所有日志 JSON 格式 + 含 request_id / user_id
- 所有 LLM 调用在 ai_audit_log 表留痕
- `/readyz` 返回 DB / Redis / LLM 健康状态

---

## 7. 关键设计决策回顾

| 决策点 | 选择 | 理由 |
|---|---|---|
| 异步化范围 | 全链路 | I/O 密集型场景异步收益最大 |
| 限流维度 | 全局+IP+用户三级 | 防恶意刷 + 公平分配 |
| 熔断策略 | 失败计数 + 半开试探 | 业界标准做法 |
| 缓存粒度 | 按 query hash | 相同 query 命中率高 |
| API 版本 | /v1 前缀 | 破坏性变更有退路 |
| 健康探针 | liveness + readiness 分离 | K8s 滚动更新必备 |
| 部署副本数 | 2 | 起步，按 QPS 扩 |
| 容器用户 | 非 root | 安全合规 |

---

## 8. 后续衔接

| 后续文档 | 与 #05 的关系 |
|---|---|
| #06 Harness 工程 | Trace / Metrics 接入依赖本文的结构化日志和审计表 |
| #07 MCP 工具层 | MCP Server 复用本文的 async 化和限流 |
| #08 双 Agent | 多 Agent 复用本文的熔断 / 限流 / 审计基础设施 |

---

## 9. 学习要点小结

1. **异步化是商业化第一步**：I/O 密集型场景异步收益最大
2. **限流 / 熔断 / 降级是三个不同概念**：限流挡入口、熔断快速失败、降级返回兜底
3. **Token 必须有预算**：LLM 成本是变量，没有预算等于敞口袋烧钱
4. **API 加版本前缀**：商业客户最怕"接口突然变了"
5. **健康探针 liveness / readiness 必须分离**：K8s 滚动更新必备
6. **非 root 容器**：安全合规硬指标
7. **优雅停机**：SIGTERM → 拒新请求 → 等在飞 → 退出，避免丢请求
