# ===== LLM =====
# LiteLLM 统一路由：推荐优先 DeepSeek（便宜 + 中文好）
# 模型名格式参考 litellm 官方：deepseek/deepseek-chat / openai/gpt-4o-mini / qwen/qwen-plus
LLM_MODEL=deepseek/deepseek-chat
LLM_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# 自建兼容端点可以填（DeepSeek 官方默认是 https://api.deepseek.com/v1 可以不填）
LLM_BASE_URL=

# ===== 现有后端数据库（只读） =====
# 与 spring boot 共用同一个 sports_takeout，账号给只读权限即可
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=sports_takeout

# ===== 现有后端 Redis（可选：推荐结果缓存 24h） =====
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=1
REDIS_PASSWORD=

# ===== HTTP 服务 =====
SERVICE_HOST=0.0.0.0
SERVICE_PORT=18000
SERVICE_ENV=dev

# ===== MCP 工具层（#07，默认关：单 Agent 单进程直接调用够用） =====
MCP_ENABLED=false
MCP_SERVER_PORT=18001
MCP_SERVER_URL=http://localhost:18001/mcp

# ===== 多 Agent（#08） =====
REVIEW_SUMMARY_ENABLED=true
CERT_REVIEW_ENABLED=true
SUPERVISOR_ENABLED=false
HITL_ENABLED=false

# ===== Checkpointer（上线加固：崩溃恢复 + 多副本共享 + DB 灾备） =====
CHECKPOINTER_BACKEND=memory     # memory（开发）/ redis（生产，多副本共享 + TTL）
CHECKPOINT_TTL_MINUTES=60
CHECKPOINT_DB_FALLBACK=true     # DB 灾备层：Redis miss 时从 DB 读 state 回填（生产默认开）

# ===== 混合检索（#04 RAG 升级） =====
# BM25 便宜且离线可跑，默认开；向量/重排依赖 bge-m3/FlagEmbedding 重模型，默认关（国内难下载）。
HYBRID_RETRIEVAL_ENABLED=true
RERANKER_ENABLED=false
BM25_TOP_K=50
RRF_K=60
RRF_TOP_K=30
# 预留：接入重模型时才需要改下面这些
EMBEDDING_MODEL=BAAI/bge-m3
EMBEDDING_DEVICE=cpu
EMBEDDING_USE_FP16=true
VECTOR_SKIP_INITIAL_UPSERT=false
RERANKER_MODEL=BAAI/bge-reranker-v2-m3
RERANKER_DEVICE=cpu
RERANKER_USE_FP16=true

# ===== 向量库后端（三选一） =====
# chroma      = Chroma PersistentClient，本地文件，零运维（开发默认）
# chroma_http = Chroma HttpClient，Docker 独立部署，多副本共享（过渡方案）
# pgvector    = PostgreSQL + pgvector 扩展，HA + 主从复制（生产首选）
VECTOR_DB_BACKEND=chroma

# —— chroma / chroma_http 模式 ——
VECTOR_DB_PATH=./data/chroma            # chroma: 本地目录 / chroma_http: "host:port" 格式

# —— pgvector 模式（VECTOR_DB_BACKEND=pgvector 时用）——
PGVECTOR_HOST=127.0.0.1
PGVECTOR_PORT=5432
PGVECTOR_USER=postgres
PGVECTOR_PASSWORD=
PGVECTOR_DATABASE=sports_takeout
PGVECTOR_TABLE=ai_vector_coach          # AI 服务专用向量表（加 ai_ 前缀与业务表隔离）
PGVECTOR_DIM=1024                       # bge-m3 输出维度=1024
