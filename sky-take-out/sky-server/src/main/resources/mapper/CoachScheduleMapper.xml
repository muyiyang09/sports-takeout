<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.CoachScheduleMapper">

    <insert id="insertBatch">
        insert into coach_schedule (coach_id, schedule_date, time_slot, status, order_id, create_time, update_time)
        values
        <foreach collection="schedules" item="s" separator=",">
            (#{s.coachId}, #{s.scheduleDate}, #{s.timeSlot}, #{s.status}, #{s.orderId}, #{s.createTime}, #{s.updateTime})
        </foreach>
    </insert>

    <select id="listByCoachAndDateRange" resultType="com.sky.vo.CoachScheduleVO">
        select * from coach_schedule
        where coach_id = #{coachId}
        <if test="startDate != null">
            and schedule_date &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            and schedule_date &lt;= #{endDate}
        </if>
        order by schedule_date asc, time_slot asc
    </select>

    <select id="listAvailableByCoachAndDate" resultType="com.sky.vo.CoachScheduleVO">
        select * from coach_schedule
        where coach_id = #{coachId}
        and schedule_date = #{scheduleDate}
        and status = 1
        order by time_slot asc
    </select>

</mapper>
