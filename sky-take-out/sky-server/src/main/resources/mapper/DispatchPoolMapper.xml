<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DispatchPoolMapper">

    <update id="update" parameterType="com.sky.entity.DispatchPool">
        update dispatch_pool
        <set>
            <if test="coachId != null">
                coach_id = #{coachId},
            </if>
            <if test="dispatchType != null">
                dispatch_type = #{dispatchType},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="dispatchTime != null">
                dispatch_time = #{dispatchTime},
            </if>
            <if test="expireTime != null">
                expire_time = #{expireTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="listSeizableByCityCode" resultType="com.sky.vo.DispatchPoolVO">
        select dp.id, dp.order_id, dp.city_code, dp.expire_time, dp.create_time,
               o.schedule_date, o.time_slot, o.address, o.consignee, o.phone, o.amount
        from dispatch_pool dp
        join orders o on dp.order_id = o.id
        where dp.status = #{statusPending}
        <if test="cityCode != null and cityCode != ''">
            and dp.city_code = #{cityCode}
        </if>
        order by dp.create_time asc
    </select>

    <!-- 管理端：查询全部派单池记录（含已派/已取消），可按状态筛选 -->
    <select id="listAllForAdmin" resultType="com.sky.vo.DispatchPoolVO">
        select dp.id, dp.order_id, dp.city_code, dp.expire_time, dp.create_time,
               dp.status, dp.dispatch_type, dp.coach_id, dp.dispatch_time,
               o.schedule_date, o.time_slot, o.address, o.consignee, o.phone, o.amount,
               o.order_number
        from dispatch_pool dp
        join orders o on dp.order_id = o.id
        <where>
            <if test="status != null">
                and dp.status = #{status}
            </if>
            <if test="cityCode != null and cityCode != ''">
                and dp.city_code = #{cityCode}
            </if>
        </where>
        order by dp.create_time desc
    </select>

</mapper>
