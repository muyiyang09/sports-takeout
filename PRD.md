# 体育外卖 · 上门私教平台 — 产品需求文档（PRD）

> 版本 v0.2（最终 MVP 边界确认） · 2026-08-23
> 底座：苍穹外卖 sky-take-out（Spring Boot 2.7.3 单体 + MyBatis + MySQL + Redis + 微信小程序）
> v0.2 变更：明确 MVP 做/不做边界、新增订单状态机图、合并派单/抢单池为单表、CourseSpec 规格字段并入 Course

---

## 1. 项目概述

### 1.1 背景与定位
国家鼓励全民减脂，催生「上门运动教练」这一新职业：教练携带便携器械（弹力带、壶铃、瑜伽垫、体脂秤等）上门，为客户提供科学的一对一/一对多运动减脂服务。行业造词称之为「**体育外卖**」。

### 1.2 本质判断
「体育外卖」虽带「外卖」二字，业务本质是 **上门服务 O2O**（教练 = 服务提供者，预约时段 + 上门地址），与「东郊到家 / 家政到家」同类，**不是** 美团式的「商品 + 骑手即时配送」。因此复用「到家服务」的领域模型，套用「苍穹外卖」的技术骨架。

### 1.3 一句话价值
**「把健身房搬到家里」** —— 用户下单约教练，教练带器械上门，做科学减脂运动。

---

## 2. 角色与端

| 角色 | 端 | 形态 | MVP 是否必需 |
|---|---|---|---|
| 用户（减脂客户） | 用户端 | 微信小程序 / H5 | ✅ |
| 教练 | 教练端 | 微信小程序（**新建，完整功能**） | ✅ |
| 平台运营 | 管理端 | PC Web（复用苍穹外卖 admin） | ✅ |
| 城市代理 | 城市端 | PC Web | ❌ 二期 |

---

## 3. 核心业务流程（MVP 闭环）

```
用户浏览课程(减脂/增肌/拉伸…) → 选教练（指定）或让平台派单（派单池抢单）
  → 选上门时段 + 地址 + 器械需求 → 生成预约订单
  → 支付（MVP 模拟支付）
  → 教练接单（指定单直接确认 / 派单池抢单）
  → 教练导航上门 → 服务中（上传训练记录/体测数据）
  → 服务完成 → 用户评价教练与课程 → 教练结算
```

### 3.1 订单状态机（双派单模型，MVP 核心）

```
                ┌──────────────────────────────────────────────────────┐
                │  下单方式：order_mode = 1 指定教练  /  2 派单池         │
                └──────────────────────────────────────────────────────┘

  [新建订单 status=1 待付款]
        │ 用户支付(模拟)
        ▼
  [status=2 待接单] ──────────────────────────────────────────────────┐
        │                                                          │
        ├─ mode=1 指定单 ─→ 教练确认接单/拒绝                       │
        │       │ 接受                                           │
        │       ▼                                                 │
        │   [status=3 待服务]                                    │
        │                                                          │
        └─ mode=2 派单池 ─→ 订单进 dispatch_pool(status=0 待派)  │
                │ 教练抢单成功 → dispatch_pool.status=1 已派        │
                │ coach_id 回填到 orders                           │
                ▼                                                 │
          [status=3 待服务] ◄────────────────────────────────────┘
                │ 教练点"开始服务"
                ▼
          [status=4 服务中] ← 教练上传训练记录/体测数据
                │ 教练点"完成服务"
                ▼
          [status=5 已完成] ← 用户评价(order_review)
                │
                ▼
          (结算闭环)

  异常分支：
   - status=2 时教练拒单 → status=7 拒单（rejection_reason）
   - status=1/2 时用户取消 → status=6 已取消（cancel_reason）
   - 派单池超时未派 → status=6 已取消（cancel_reason="派单超时"）
```

**状态码定义**：1待付款 / 2待接单 / 3待服务 / 4服务中 / 5已完成 / 6已取消 / 7拒单
（MVP 不做退款分支，二期加 8退款中/9已退款）

---

## 4. 功能模块清单

### 4.1 用户端（小程序）
- [x] 微信登录、地址管理（复用苍穹外卖）
- [x] 课程浏览 / 分类 / 详情
- [x] 教练列表：按评分 / 等级 / 价格筛选（MVP 不做距离筛选）
- [x] **两种下单**：指定教练 / 派单池抢单（用户视角：选具体教练 or 选"就近派单"）
- [x] 时段预约（日期 + 时段，教练排期防冲突）
- [x] 下单 + 支付（MVP 模拟支付）
- [x] 订单状态跟踪、历史订单
- [x] 服务完成评价（教练 + 课程双维度）

### 4.2 教练端（小程序，**新建，完整功能**）
- [x] 教练入驻 / 资质上传（证书）+ 管理端审核
- [x] 个人资料、可服务时段排期、服务半径
- [x] 接单（指定单确认 / 派单池抢单）
- [x] 订单导航（MVP 用地址文本 + 电话联系，二期接地图）
- [x] 服务开始/完成、训练记录上传、体测数据上传
- [x] 查看评价与评分

### 4.3 管理端（PC，复用苍穹外卖 admin）
- [x] 教练审核（资质/证书）、教练等级
- [x] 课程分类 / 课程管理（替代菜品/套餐）
- [x] 订单管理、派单池监控
- [x] 评价管理
- [ ] 数据统计（复用苍穹外卖报表，**二期，MVP 不做**）

---

## 5. 领域模型映射（苍穹外卖 → 体育外卖）

| 苍穹外卖（原） | 体育外卖（改） | 说明 |
|---|---|---|
| Dish 菜品 | **Course 课程** | 减脂/增肌/拉伸/产后恢复… |
| DishFlavor 口味 | **CourseSpec 课程规格** | v0.2：**规格字段（强度/器械/人群/时长）直接并入 Course，不独立建表**；二期如需 SKU 多价再拆 |
| Setmeal 套餐 | **CoursePackage 训练套餐** | N 节课打包 |
| SetmealDish 套餐菜品 | **PackageCourse 套餐课程** | 关联表 |
| Category 分类 | Category 分类 | 复用 |
| ShoppingCart 购物车 | **删除** | 预约不是购物车模型 |
| Orders 订单 | **Orders 预约订单** | 加：教练、时段、上门地址、派单模式、状态机 |
| OrderDetail 明细 | OrderDetail 明细 | 加：教练、时段 |
| Employee 员工 | **Employee 管理员 / Coach 教练** | 拆分（Coach 独立表） |
| User 用户 | User 用户（客户） | 复用 |
| AddressBook 地址 | AddressBook 上门地址 | 复用 |

### 5.1 新增实体（苍穹外卖没有）
| 实体 | 说明 |
|---|---|
| **Coach 教练** | 姓名/性别/头像/等级/评分/服务半径/接单状态 |
| **CoachCertificate 教练资质** | 证书图片/证书类型/审核状态 |
| **CoachSchedule 教练排期** | 日期 + 时段 + 是否可约 |
| **DispatchPool 派单/抢单池** | v0.2：**DispatchPool 与 SeizePool 合并为单表**，用 `dispatch_type` 字段区分（0待派 1系统派 2教练抢单）；支持「随机派单」与「教练抢单」两种 |
| **OrderReview 评价** | 教练 + 课程双维度评分/图文 |

---

## 6. 数据库设计（表清单）

**保留复用**：`user`、`address_book`、`category`、`orders`（改造）、`order_detail`（改造）、`employee`（改 admin）

**改造重命名**：`dish` → `course`（含规格字段）、`setmeal` → `course_package`、`setmeal_dish` → `package_course`
（`dish_flavor` 不再独立建表，规格字段并入 `course`）

**删除**：`shopping_cart`

**新增**：`coach`、`coach_certificate`、`coach_schedule`、`order_review`、`dispatch_pool`（抢单池合并入此表）

**最终表清单（14 张）**：
1. employee（管理员）
2. user（用户）
3. address_book（上门地址）
4. category（分类）
5. coach（教练）
6. coach_certificate（教练资质）
7. coach_schedule（教练排期）
8. course（课程，含规格字段）
9. course_package（训练套餐）
10. package_course（套餐课程关联）
11. dispatch_pool（派单/抢单池）
12. orders（预约订单，改造）
13. order_detail（订单明细，改造）
14. order_review（评价）

---

## 7. 技术栈与底座

- 后端：Spring Boot 2.7.3（继承苍穹外卖）、MyBatis、MySQL、Redis、JWT、Knife4j、PageHelper、阿里云 OSS
- 前端：uniapp 微信小程序（用户端改造 + 教练端新建）、PC 管理端（继承苍穹外卖 admin，Vue + ElementUI 风格）
- 支付：MVP 阶段 **模拟支付**，二期接微信支付
- 地图：MVP 用地址文本，二期接高德/百度定位导航
- 通信：WebSocket（来单提醒，复用苍穹外卖 WS 模块）

---

## 8. 实施计划（分阶段）

1. **阶段一：数据库** —— 写 schema.sql（含种子数据）✅ 本轮交付
2. **阶段二：后端领域层** —— 重写 entity/DTO/VO + mapper + service + controller
   - 课程模块（替代菜品）
   - 教练模块（资质/排期/接单）
   - 预约订单模块（时段 + 上门 + 状态机）
   - 派单/抢单模块（合并 dispatch_pool）
   - 评价模块
3. **阶段三：管理端接口** —— 教练审核、课程管理、订单管理、派单监控
4. **阶段四：用户端小程序** —— 改造页面（课程→教练→预约→订单→评价）
5. **阶段五：教练端小程序** —— 新建（接单/排期/导航/服务记录）
6. **阶段六：联调 + 模拟支付 + 种子数据**

---

## 9. 待办 / 风险

- 教练端为新增，苍穹外卖无对应端，**工作量最大**
- **双派单模型状态机复杂**：指定单（mode=1）走教练确认流程，派单池（mode=2）走抢单池流程；两分支需在 service 层用策略模式区分，避免 if-else 堆叠
- 微信小程序需开发者账号 + appid（可先用 H5 调试）
- 减脂服务涉及健康风险，需加身体状态问卷 + 免责协议（二期）
- 派单池超时未派的兜底逻辑需用 Spring Task 定时任务（MVP 实现超时自动取消）

---

## 10. MVP 边界（做 / 不做）

### 10.1 做（完整闭环）
- 3 端：用户端小程序 + 教练端独立小程序（完整功能）+ 管理端 PC
- 实体全保留：Coach / CoachCertificate / CoachSchedule / Course（含规格字段）/ CoursePackage / PackageCourse / DispatchPool（含抢单）/ OrderReview
- 双派单：指定教练（直接确认）+ 派单池抢单，两种共存
- 教练入驻：自助提交资料 + 证书上传 + 管理端审核
- 评价：教练 + 课程双维度
- 服务记录：教练上传训练记录 / 体测数据
- WebSocket：来单提醒（教练端）
- 模拟支付
- 时段预约：教练排期防冲突

### 10.2 不做（推到二期）
| 项 | 二期方案 |
|---|---|
| 数据统计报表 | 复用苍穹外卖报表简化版 |
| 地图导航 | MVP 用地址文本 + 电话联系 |
| 城市代理端 | PC Web，二阶段 |
| 微信真实支付 | MVP 模拟支付 |
| 身体状态问卷 + 免责协议 | 健康风险相关，二期 |
| 退款流程 | MVP 不做退款分支（仅取消/拒单） |
| CourseSpec 独立 SKU 表 | 规格字段并入 Course，二期如需 SKU 多价再拆 |
| 教练距离筛选/排序 | MVP 用城市码粗筛 |
